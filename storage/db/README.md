# Storage Mirror Database

This directory mirrors the immutable pipeline logs from `.workspace/indexes/`. Security tooling and offline auditors can ingest the mirrored ledgers without touching the working directory, guaranteeing an immutable source of truth.
