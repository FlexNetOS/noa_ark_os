# Mobile Adaptive Renderer Stack

Mobile shells rely on the shared native renderer to produce React Native view
hierarchies.  The research notebook bridge emits a condensed stack layout that
remains thumb-friendly while preserving access to citations and media assets.
