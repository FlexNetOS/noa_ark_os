# Web Adaptive Renderer Stack

This directory wires shared shell components with web specific renderers. The
research notebook registration composes the shared widget registry with the new
`@noa-ark/research-notebook` package so notebooks hydrate inside the browser shell.
