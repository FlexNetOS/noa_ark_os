# CODEX Provider Instructions

- **Interface preference:** When possible, operate Codex via the CLI workflow instead of VS Code extensions. Only use IDE extensions when CLI access is unavailable and note the justification in the task log.
- **Policy source:** This directory carries no bespoke rules. All provider/agent guidance is defined in `AGENT.md` per `.github/AGENT_POINTER.txt`.
