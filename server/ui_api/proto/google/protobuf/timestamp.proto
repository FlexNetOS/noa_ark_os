syntax = "proto3";

package google.protobuf;

// Minimal copy of google.protobuf.Timestamp to satisfy local builds without
// relying on external protobuf distributions.
message Timestamp {
  int64 seconds = 1;
  int32 nanos = 2;
}
