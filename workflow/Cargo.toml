[package]
name = "noa_workflow"
version = "0.1.0"
edition = "2021"
authors = ["NOA ARK OS Team"]
description = "Unified workflow engine for NOA ARK OS"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
