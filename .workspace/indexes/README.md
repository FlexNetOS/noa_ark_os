# Immutable Pipeline Indexes

This directory stores append-only ledgers that are generated by the relocation and documentation pipelines. Each entry is mirrored to `storage/db/` and chained with the previous signature, enabling tamper-evident verification for auditors and policy enforcement tooling.
