# Claims Table — Launch Codex

| ID | Claim | Evidence | Verification | Limits |
|----|-------|----------|--------------|--------|
| C1 | `noa-unified-server` now boots the orchestrator, gateway, and Axum/Tonic API through the CLI profile without merge markers. | `server/bins/noa-unified-server/Cargo.toml` (`be4281812ff4cd218239ebb8c4691542669e65cdc78a22cac58ff05d807a06ac`) and `server/bins/noa-unified-server/src/main.rs` (`694c65fa27e27305adcb272d4cd94662582c7e898c1d9d1d05fd03e8713bc4fa`). | Exercised indirectly via `bash scripts/codex-bootstrap.sh` which compiled dependent crates successfully. | Only validates the unified server binary builds on Linux; Windows/macOS still untested. |
| C2 | `scripts/codex-bootstrap.sh` installs Codex CLI + `noa-mcp-server` and patches `~/.codex/config.toml`. | Script hash `191397b9b395a021f21ca8e1bc5de33a1b5aef71e5d4929ea55a85866be23039`; run log shows codex + noa-mcp-server install succeeded. | `bash scripts/codex-bootstrap.sh` (exit 0 at 2025-11-20T05:54:28Z). | Requires local PATH to include `$HOME/.local/bin`; script warns but does not mutate shell rc files. |
| C3 | `cicd/scripts/codex-mcp-health.sh` now spawns `noa-tools-agent`, verifies `/list_files`, and exercises the MCP stdio bridge. | Script hash `cbc66f9eb5068cb9b1d2ca7be6bfc2cd212b99918c97782e3a453caacb785a3a`. | `PATH="$HOME/.local/bin:$PATH" bash cicd/scripts/codex-mcp-health.sh` (exit 0 at 2025-11-20T05:54:28Z) confirmed `noa.list_files` RPC returns entries. | Health check currently covers default workspace path only; remote NOA_TOOLS_SERVER_URLs remain untested. |
| C4 | `server/Cargo.toml` now treats `server/mcp` as a workspace member so `cargo install --path server/mcp` succeeds. | File hash `d355d5f3fedf31cd9dae08f548ca0c215c853ce54455e331d52fd246f80268a4`. | Implicitly validated by the bootstrap script’s `cargo install` run succeeding. | Does not yet add the crate to the root workspace; top-level inclusion pending broader workspace governance. |
