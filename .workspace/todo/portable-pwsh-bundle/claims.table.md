# Claims Table — Portable PowerShell Bundle

| ID | Claim | Evidence | Verification | Limits |
|----|-------|----------|--------------|--------|
| C1 | Portable PowerShell 7.4.5 bundle is installed offline with manifest + SHA recorded. | `server/tools/pwsh-portable.manifest.json` (sha256 `6ff2655ae5773baf86c724964ba74d0aea22ed38e8c90cc77cebeb0f54593a5c`) + bundle at `server/tools/pwsh-portable/current/`. | `tools/automation/check_portable_pwsh.py --output text` (exit 0 at 2025-11-21T11:01Z) confirms manifest + binary hash match. | Bundle build currently targets linux-x64; Windows/macOS archives require re-running `setup-portable-pwsh.ps1`. |
| C2 | Activators now export `POWERSHELL_BIN` automatically for launcher + toolchains. | `server/tools/activate-pwsh.{sh,ps1}` plus updates to `activate-cargo-wsl.sh`, `activate-node.sh`, and `scripts/full_stack_launch.sh` (powershell_activation phase evidence in `build_output/system-launch/pwsh-activation.json`). | `bash scripts/full_stack_launch.sh --prepare-only --skip-tests --skip-docker --skip-make --skip-notebook --cuda-mode auto --llama-mode auto --master-controller-mode skip --pipeline-mode skip` (exit 0 at 2025-11-21T11:01Z) recorded `status=activated` in `build_output/system-launch/pwsh-activation.json`. | Launcher still skips CUDA/Llama on non-Windows hosts because those provisioning scripts expect Windows installers. |
| C3 | CI guard fails fast when the portable bundle is missing or tampered. | `scripts/lib/ensure_no_duplicate_tasks.sh` now runs `tools/automation/check_portable_pwsh.py`, emitting a manifest claim. | `scripts/lib/ensure_no_duplicate_tasks.sh` (exit 0 at 2025-11-21T11:01Z) logged `CLAIM portable_pwsh_manifest status=ok …` and would exit 13 if the bundle were absent. | Only validates manifest + binary hash; does not download replacements or repair the bundle. |
| C4 | Toolchain registry + workspace memory now track the PowerShell capability for downstream automation. | `.workspace/registry/toolchains.json` + updated `WORKSPACE_MEMORY.md` section “Portable PowerShell Bundle (2025-11-21)”. | Documentation change reviewed alongside launcher run above. | Memory and registry describe linux activation; cross-platform owners must extend entries as additional bundles land. |
