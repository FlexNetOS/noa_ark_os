# Claims Table — Portable PowerShell Bundle

| ID | Claim | Evidence | Verification | Limits |
|----|-------|----------|--------------|--------|
| C1 | Portable PowerShell 7.4.5 bundles for Linux + macOS (x64/arm64) + Windows are archived offline with a single manifest hash for automation evidence. | `server/tools/pwsh-portable.manifest.json` (sha256 `d34317bef0eb9bb49fbc2cf0a37c055c24d1706b068376b451bf48d569b1545e`) generated via `PWSH_INCLUDE_DESKTOP=1 bash server/tools/setup-portable-pwsh.sh --platforms host`. | `tools/automation/check_portable_pwsh.py --platform linux-x64 --ensure-exec --require-current --output text` (exit 0 at 2025-11-21T15:45Z) confirms manifest hash plus current/bin linkage. | Desktop bundles are packaged but not exercised on this host; consumers must copy the manifest+archives to native machines before use. |
| C2 | Toolchain activators expose a single entry point (`server/tools/activate-toolchains.sh`) that brings up pwsh/cargo/node and records launcher evidence (`pwsh-activation.json`). | `server/tools/activate-toolchains.sh`, `activate-{pwsh,cargo-wsl,node}.sh`, and `scripts/full_stack_launch.sh` (phase summary prints manifest/binary/current hashes). | `bash scripts/full_stack_launch.sh --prepare-only --skip-tests --skip-docker --skip-make --skip-notebook --cuda-mode auto --llama-mode auto --master-controller-mode skip --pipeline-mode skip` (exit 0 at 2025-11-21T15:52Z) emitted `status=activated` plus manifest SHA inside `build_output/system-launch/pwsh-activation.json`. | Launcher prep run stops before starting long-running services; full-stack mode still requires real models to keep inference online. |
| C3 | Duplicate-task guard enforces the manifest via the checker so CI fails fast with remediation guidance. | `scripts/lib/ensure_no_duplicate_tasks.sh` shelling out to `tools/automation/check_portable_pwsh.py --ensure-exec --require-current`. | `scripts/lib/ensure_no_duplicate_tasks.sh` (exit 0 at 2025-11-21T15:46Z) logged the `CLAIM portable_pwsh_manifest …` line; tampering would exit 13 instructing operators to rerun the setup script with `--include-desktop`. | Guard only validates integrity; it does not repair missing archives automatically. |
| C4 | CUDA + llama orchestration scripts now run under portable PowerShell on Linux, so the launcher no longer treats those phases as Windows-only. | `scripts/setup/setup-cuda.ps1`, `scripts/dev/setup-llama-cpp.ps1`, `scripts/dev/start-llama-server.ps1`, placeholder GGUF (`server/ai/llama-cpp/models/Llama-3.2-3B-Instruct.Q4_K_M.gguf`) for automation, and the launcher’s phase summary showing CUDA/Llama prerequisites satisfied. | Same launcher prep run as C2 (exit 0 at 2025-11-21T15:52Z) recorded `cuda=ran` and `llama_server=planned`; `scripts/dev/setup-llama-cpp.ps1 -SkipModelDownload` executed under portable pwsh beforehand. | Placeholder GGUF is not a usable model; real inference still requires downloading an actual quantized checkpoint plus GPU drivers. |
